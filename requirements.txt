# Core data science and ML libraries
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0

# Sentence transformers for embeddings
sentence-transformers>=2.2.0

# UMAP for dimensionality reduction and visualization
umap-learn>=0.5.0

# Matplotlib for plotting
matplotlib>=3.5.0

# Anthropic API for Claude integration
anthropic>=0.3.0

# Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.0.0

# Standard library dependencies (usually included with Python)
# base64, json, os, sys, re, pathlib, collections, dataclasses, typing
# These are part of Python standard library and don't need to be specified

# Optional: For better performance and compatibility
# Uncomment if needed:
# torch>=1.12.0  # for sentence-transformers backend
# transformers>=4.20.0  # for sentence-transformers
